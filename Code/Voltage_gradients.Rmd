---
title: "Voltage_gradients"
output: html_document
---
# Introduction

This rmd. file imports voltage data from Google sheet
Data is used to plot a voltage gradient map of the winogradsky column 

#Loading Packages 

```{r loading packages}
library(tidyverse) 
library(googledrive) 
library(googlesheets4)
library(ggplot2)
library(knitr)
```

#Importing from google sheet

```{r load CleanVoltageData direct from googlesheet}

googlesheets4::gs4_deauth()

CleanData_Voltage <- googlesheets4::read_sheet("https://docs.google.com/spreadsheets/d/1hNakCyqPsTNRFpf1lGjoBQ-papyZ6xHKT6mcvh3LMb8/edit?pli=1&gid=842906991#gid=842906991", "CleanData_Voltage") 
kable(CleanData_Voltage)
```

#Voltage Gradient Plot

```{r Voltage Gradients in water at 10 seconds}

ggplot(data = CleanData_Voltage) +
geom_point(aes(y = Depth_cm, x = Voltage_Water_10s)) + labs( title= "Voltage Gradient in water at 10 seconds", caption= "Figure X. Voltage (mv) measured in four Winogradsky colums at 5 depths (cm)") + theme(plot.caption= element_text(size = 11, hjust=0)) +
  facet_wrap(vars(unlist(Sample)))



```

```{r Voltage Gradients in water at maximum}

ggplot(data = CleanData_Voltage) +
geom_point(aes(y = Depth_cm, x = Voltage_Water_Max)) + labs( title= "Voltage Gradient in water at maximum", caption= "Figure X. Voltage (mv) measured in four Winogradsky colums at 5 depths (cm)") + theme(plot.caption= element_text(size = 11, hjust=0)) +
  facet_wrap(vars(unlist(Sample)))
```

```{r Voltage Gradients grounded on tap at 10 seconds}

ggplot(data = CleanData_Voltage) +
geom_point(aes(y = Depth_cm, x = Voltage_Tap_10s)) + labs( title= "Voltage Gradient grounded to tap at 10 seconds", caption= "Figure X. Voltage (mv) measured in four Winogradsky colums at 5 depths (cm)") + theme(plot.caption= element_text(size = 11, hjust=0)) +
  facet_wrap(vars(unlist(Sample)))
```
```{r Voltage Gradients grounded on tap at maximum}

ggplot(data = CleanData_Voltage) +
geom_point(aes(y = Depth_cm, x = Voltage_Tap_Max)) + labs( title= "Voltage Gradient in water at 10 seconds", caption= "Figure X. Voltage (mv) measured in four Winogradsky colums at 5 depths (cm)") + theme(plot.caption= element_text(size = 11, hjust=0)) +
  facet_wrap(vars(unlist(Sample)))

```

